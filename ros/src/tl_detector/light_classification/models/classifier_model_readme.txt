Traffic light classifier model file (.hdf5) should be placed in this folder.
The current working model is:

ResNet50-UdacityRealandSimMix-Best-val_acc-1.0.hdf5

Split-up model files will automatically be recombined on first run if needed,
but can also be manually recombined by the following commands:

python model_file_join.py